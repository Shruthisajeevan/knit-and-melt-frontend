# Local development — points to local Django
VITE_API_URL=http://localhost:8000/api

# Production — replace with your Render URL after deploying
# VITE_API_URL=https://knit-and-melt-api.onrender.com/api
